CISCO REFERENCE STRUCTURE AND OUTPUT — v2
These requirements are already included in 01_Project_Instructions.txt. This file is a standalone reminder for older projects; do not append duplicate copies to v2.

Use the working REFERENCE_CISCO as the structural template. Preserve actual section sequence, grouping, indentation, interface style and ! separators. Derive target behavior and site/device-specific values from TARGET_JUNIPER and explicit TARGET_PLAN changes. Retain applicable approved reference patterns with their dependencies, not unrelated reference services or identities.
Return ONE continuous complete configuration code block. No topic headings or explanation inside, no ellipses, no omitted interface repetitions, no 'same as reference'. Omit show-command banners; do not add execution wrappers unless requested. Put target features absent from the reference beside related configuration and document those additions afterward.
Check both reference-order fidelity and target-feature coverage before returning. Missing source sections remain UNVERIFIED. Unresolved values require INCOMPLETE — NOT DEPLOYABLE outside the block and a complete unresolved-item list. Never invent values or portable credential conversions. Provide a complete text file only when supported; disclose an output limit rather than silently truncating.
